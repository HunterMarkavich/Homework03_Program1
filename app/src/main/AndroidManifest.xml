<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homework03_Program1"
        tools:targetApi="31">

        <!-- MainActivity declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NewStudentScreen activity declaration -->
        <activity android:name=".NewStudentScreen" />

        <!-- NewMajorScreen activity declaration -->
        <activity android:name=".NewMajorScreen" />

        <!-- SearchStudentScreen activity declaration -->
        <activity android:name=".SearchStudentScreen" />

        <!-- ViewStudentScreen activity declaration -->
        <activity android:name=".ViewStudentScreen" />

        <!-- UpdateStudentInfoScreen activity declaration -->
        <activity android:name=".UpdateStudentInfoScreen" />

    </application>

</manifest>
